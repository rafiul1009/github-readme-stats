# GitHub API Token for authentication
GITHUB_TOKEN=your_github_token_here

# Optional: comma-separated GitHub usernames this instance will serve.
# Unset (default) serves everyone. Only meaningful for a self-hosted deployment.
# WHITELIST=your-username,another-user
